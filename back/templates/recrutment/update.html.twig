{% extends 'base.html.twig' %}

{% block title %}Liste recrutement!{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_row(form.post) }}
    {{ form_row(form.profil) }}
    {{ form_row(form.mission) }}
    <input type="submit" value="Editer" class="btn btn-success">
    {{ form_end(form) }}

{% endblock %}